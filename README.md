`Windows - System32 - wbem - WMIC.exe`  

`wmic.exe` is still shipped with both Windows PE, and the Windows Setup.  
`professional_en-us.esd` (get the link from official Microsoft servers, or UUPDump download script for aria2 under `files` folder),  
and extract it using 7zip.  

this is `v10.0.0.28000` from 26H1 build `28000.1764` (English `amd64`)

your script should work fine with that `wmic.exe` other than showing a little red message saying the `wmic.exe is deprecated` each time you run it.  
